fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
psycopg2-binary==2.9.10
alembic==1.14.1
python-dotenv==1.0.1
pydantic-settings==2.7.1
pydantic[email]==2.10.5
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-multipart==0.0.20
openpyxl==3.1.5
xlrd==2.0.1
msoffcrypto-tool==5.4.2
pandas==2.2.3
httpx==0.28.1
aiosmtplib==5.1.0
email-validator==2.2.0
apscheduler==3.10.4
anthropic>=0.40.0
pdfplumber>=0.11
pytesseract>=0.3.10
pdf2image>=1.17
beautifulsoup4>=4.12
playwright>=1.48
cryptography>=43
twilio>=9
paramiko==3.5.0

# Windows-only: the Phoenix green-screen driver (scripts/windows/*) drives a native
# PowerTerm window via win32gui/win32api SendInput and grabs it via PIL. These were
# never declared — they only existed on the dev machine, so every OTHER worker's venv
# lacked them and phoenix_win_terminal died on `import win32gui` the moment the
# terminal opened. Markers keep them off the Linux/Railway build.
pywin32>=306; sys_platform == "win32"
Pillow>=10; sys_platform == "win32"
